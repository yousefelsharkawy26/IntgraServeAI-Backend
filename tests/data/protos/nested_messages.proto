syntax = "proto3";

package test;

service TestService {
  rpc DoSomething (OuterMessage) returns (OuterMessage);
}

message OuterMessage {
  message InnerMessage {
    string value = 1;
  }
  InnerMessage inner = 1;
}
